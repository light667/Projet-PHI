# Build Firebase Hosting : URL du backend Render (sans slash final).
# Optionnel si tu gardes le fallback dans `src/lib/api.ts` ; sinon obligatoire au `npm run build`.

VITE_API_URL=https://phi-api-dt94.onrender.com

# Sur Render (backend), utiliser le nom exact : CORS_ORIGINS = par ex.
#   https://phi-org.web.app,http://localhost:5173,http://127.0.0.1:5173

# En local uniquement :
# VITE_API_URL=http://localhost:8000
