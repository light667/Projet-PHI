# Build Firebase Hosting : URL du backend Render (sans slash final).
# Copier vers `.env` / `.env.production` avant `npm run build`, ou définir la variable dans la CI Firebase.

# Backend Render (prod) :
VITE_API_URL=https://phi-api-dt94.onrender.com

# En local, tu peux surcharger :
# VITE_API_URL=http://localhost:8000
