# Build Firebase Hosting : définir l’URL du backend Render (sans slash final).
# Firebase Console → Hosting → environnements, ou fichier .env avant `npm run build`.
#
# Exemple après création du service sur Render :
# VITE_API_URL=https://phi-api.onrender.com

VITE_API_URL=
