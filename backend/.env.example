# Copier vers .env pour le développement local (ne jamais committer .env).
# Sur Render : définir les mêmes clés dans le tableau Environment du service.

# Origines autorisées pour CORS (séparées par des virgules, sans espace après la virgule si possible).
CORS_ORIGINS=https://phi-org.web.app,http://localhost:5173,http://127.0.0.1:5173

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

GEMINI_API_KEY=
